pkgname=rdkit-postgresql
_pkgname=RDKit
pkgver=2016_03_1
pkgrel=1
pkgdesc="RDKit - A collection of cheminformatics and machine-learning software written in C++ and Python."
arch=("i686" "x86_64")
url="http://rdkit.org/"
license=('New BSD License')
depends=( 'rdkit-python' )
source=("https://github.com/rdkit/rdkit/archive/Release_${pkgver}.tar.gz" Makefile.patch)
sha256sums=('c0ab786ba745bb355141875e6b7fcc94a55cb8709fdc3508e38575b228f668f1' SKIP)
provides=('rdkit-postgresql')
srcpath=/rdkit-Release_${pkgver}/Code/PgSQL/rdkit

prepare() {
    cd $srcdir/$srcpath
    patch -i $srcdir/Makefile.patch
}

build() {
    cd $srcdir/$srcpath
    make USE_THREADS=1
}

package() {
    cd $srcdir/$srcpath
    make DESTDIR=${pkgdir} install
}
